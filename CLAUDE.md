<!-- vault:start -->
## Vault context for this project

Prior Topic and Decision notes in your Obsidian vault relevant to work in this directory:

- [[AgentCommander code reference surveys]] — Two precise survey reports of AgentCommander internals exist in the session archive. Both were generated by Haiku with explicit asks for lin
- [[AgentCommander project overview]] — AgentCommander is a stdlib-only Python CLI for local multi-agent LLM orchestration. Source at `C:\Users\sixoffive\Documents\AgentCommander\`
- [[AndroidMusicPlayer Spotify-style local player]] — `AndroidMusicPlayer` is a Jetpack Compose Android music player with a Spotify-style UI, playing local files via Media3 / ExoPlayer and index
- [[EngineCommander platform overview]] — EngineCommander is a Node.js/TypeScript backend + vanilla-JS PWA that provides subscription-based access to a multi-provider LLM orchestrati
- [[LLM pretraining strategy for AgentCommander orchestrator]] — Detailed corpus + training-stage plan for an LLM specialized as the AgentCommander orchestrator (the per-iteration JSON-schema decision-make
- [[Windows console mouse input for Python TUIs]] — > **Outcome update (2026-05-08):** the AgentCommander TUI mouse implementation was **reverted** after this work landed — too many bugs in th
- [[llama.cpp VRAM optimization for MoE models]] — How to fit a 35B-A3B MoE (e.g. Qwen3.6-35B-A3B) into 16GB VRAM with usable speed. Drop-in technique pack from llama.cpp / llama-server. Vali

**Before answering non-trivial questions about this project**, call `semantic_search` (Ollama nomic-embed-text) or `search_topics` (lexical) via the `obsidian-vault` MCP server — even if it looks like you already know the answer. The vault often has specific decisions, infrastructure details, and tradeoffs you wouldn't reconstruct without it. Cite results inline as `[[Topic name]]` wikilinks. See the vault-root `CLAUDE.md` recall protocol for details.

_Auto-generated by `vault-maintenance.py projects` on 2026-06-02. Do not hand-edit between the markers; changes will be overwritten._
<!-- vault:end -->
